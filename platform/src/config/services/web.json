{
  "client": {
    "gqlUrl": "{{ gql.public }}",
    "secure": true,
    "chatwoot": {
      "enabled": true,
      "url": "{{ chatwoot.url }}",
      "websiteToken": "{{ chatwoot.websiteToken }}"
    }
  },

  "server": {}
}