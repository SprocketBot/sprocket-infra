{
  "client": {
    "gqlUrl": "{{ api.url }}",
    "secure": true,
    "chatwoot": {
      "enabled": true,
      "url": "https://chatwoot.spr.ocket.cloud",
      "websiteToken": "{{ chatwoot.websiteToken }}"
    }
  },

  "server": {},

  "stack": "{{ stack }}"
}
