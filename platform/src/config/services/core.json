{
  "transport": {{{transport}}},
  "gql": {
    "playground": false
  },
  "logger": {
    "levels": {{{logger.levels}}}
  },
  "minio": {
    "endPoint": "{{s3.endpoint}}",
    "port": {{ s3.port }},
    "useSSL": {{ s3.ssl }},
    "bucketNames": {
      "replays": "{{s3.buckets.replayParse}}",
      "image_generation": "{{s3.buckets.imageGeneration}}"
    }
  },
  "redis": {
    "port": {{redis.port}},
    "host": "{{ redis.host }}",
    "prefix": "{{ redis.prefix }}"
  },
  "db": {
    "host": "{{database.host}}",
    "port": {{database.port}},
    "username": "{{database.username}}",
    "database": "{{database.database}}",
    "enable_logs": false
  },
  "auth": {
    "discord": {
      "callbackUrl": "https://{{ api.url }}/login"
    },
    "google": {
      "callbackUrl": "https://{{ api.url }}/google/redirect"
    },
    "jwt_expiry": 12000,
    "frontend_callback": "https://{{ frontend.url }}/auth/callback"
  },
  "defaultOrganizationId": 2
}
