{
  "db": {
    "host": "mlesports.dev",
    "port": "56497",
    "username": "root",
    "database": "MLEDB_DEV",
    "enable_logs": false
  },
  "transport": {{{transport}}},
  "logger": {
    "levels": {{{logger.levels}}}
  },
  "minio": {
    "endPoint": "{{s3.endpoint}}",
    "port": {{ s3.port }},
    "useSSL": {{ s3.ssl }},
    "bucketNames": {
      "replays": "{{s3.bucket}}"
    }
  }
}
