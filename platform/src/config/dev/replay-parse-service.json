{
  "celery": {
    "broker": "{{ celery.broker }}",
    "backend": "{{ celery.backend }}",
    "queue": "{{ celery.queue }}"
  },
  "transport": {
    "url": "amqp://{{ rmq.host }}",
    "analytics_queue": "dev-analytics"
  },
  "minio": {
    "hostname": "{{ s3.endpoint }}",
    "bucket": "{{ s3.bucket }}",
    "parsed_object_prefix": "dev",
    "accessKey": "{{ s3.accessKey }}"
  },
  "tempDir": "/tmp"
}
